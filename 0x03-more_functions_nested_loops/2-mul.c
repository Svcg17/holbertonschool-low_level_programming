#include "holberton.h"
/**
 * mul - multiply two integers.
 * @a: integer taken to multiply.
 * @b: integer taken to multiply.
 * Return: 0.
 */
int mul(int a, int b)
{
int m;
m = a * b;
return (m);
}
